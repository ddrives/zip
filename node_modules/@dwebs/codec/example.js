var bufKeyMaybe = require('@dwebs/buffer-key')

var key = bufKeyMaybe(process.argv[2])

try {
  console.log('maybe a key: key valid or null', bufKeyMaybe(key))
} catch (e) {
  console.log('key not valid')
}
